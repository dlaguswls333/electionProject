# Use this for production builds, where the browser calls the API directly.
# Example: https://api.example.com
VITE_API_BASE_URL=

# Use this in local development to avoid browser CORS through the Vite proxy.
# Keep VITE_API_BASE_URL empty when using this.
VITE_API_PROXY_TARGET=http://localhost:8080
